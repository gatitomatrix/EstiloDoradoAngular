# Web en Render

Los pasos completos están en el repo Laravel: `RENDER_PASOS.md`.

Resumen de este repo:

- Auto-Deploy: **No**
- Build: `npm ci && npm run build`
- Publish: `dist/estilo-dorado/browser`
- Variable: `API_BASE_URL` = `https://TU-API.onrender.com/api`

`ng serve` en tu PC sigue usando `localhost`. No cambies `environment.ts`.
